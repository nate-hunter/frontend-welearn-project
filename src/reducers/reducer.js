const initialState = {

}

const reducer = (state=initialState, action) => {
  switch (action.type) {

    case: "ALL_CAPS"
      return state

    default:
      return state
  }
}

export default reducer;
