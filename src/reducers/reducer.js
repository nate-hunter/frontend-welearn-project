const initialState = {

}

const reducer = (state=initialState, action) => {
  switch (action.type) {

    case: "LOAD_USER"
      return state

    default:
      return state
  }
}

export default reducer;
